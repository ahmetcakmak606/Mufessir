// Tek kanonik embedding modeli. Mühürlü: değiştirilemez (uzay tutarlılığı = makale geçerliliği).
// 3-large default 3072 döndürür; dimensions=1536 ZORUNLU (vector(1536) kolonu + 3-small'la aynı boyut).
export const EMBEDDING_MODEL = "text-embedding-3-large";
export const EMBEDDING_DIMENSIONS = 1536;
