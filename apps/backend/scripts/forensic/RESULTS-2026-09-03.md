# KITE forensik — chunking eşiği + all_tafsirs boşaltma (2026-09-03)

`kite-chunk-invariant.ts` (read-only). Soru: (a) chunk eşiği tam nedir? (b) chunk'lanınca all_tafsirs.embedding boşaltılıyor mu, yoksa iki tabloda da mı duruyor?

## (a) Eşik: TOKEN temelli (cl100k > 8192), char değil

- Operatif kural (`generate-chunks.ts:21,173-174`): `cl100k_base` token > **8192** → chunk'lanır. `if (tokenCount <= TOKEN_LIMIT) continue;`. Chunk paketleme: hedef 6500, sert tavan 8000 token.
- `generate-chunks.ts:156,167`'deki `LENGTH(commentary) > 20000` **operatif sınır DEĞİL** — yalnız o betiğin kaba aday ön-filtresi. Kanıt: 16392 / 16426 / 19598 char'lık Core-5 satırları chunk'lı (bu betik onları >20000 filtresiyle atlardı). Chunk'ların çoğu 06-12 toplu yüklemesiyle (upstream token-gate) gelmiş; generate-chunks yalnız >20000 char'lık çok uzunlara `id NOT IN (parent_id)` ile üstüne ekleme yapıyor. Tek INSERT eden betik generate-chunks olsa da 6005 chunk'lı parent'ın hepsini o üretmemiş.
- char yalnız proxy (gözlenen char/token ≈ **1.39**, `former/embed-test-verses.ts:58`). 8192 token ≈ 11.4k char.
- **Ampirik crossover (global):** chunk'lı parent min char = **9307**, max = 619377; chunk'sız-dolu parent min = 56, max = **11696**. Yani ~9.3k–11.7k arası bulanık geçiş bandı (token varyansı). Sabit char eşiği yok. Önceki "~24k char" ifadem yanlıştı.

Koşum ayetleri × Core-5'te crossover: chunk'sız (whole) en uzun = 11110 char (24-35 İbn Kesîr); chunk'lı en kısa = 16392 char (3-7 İbn Kesîr, 2 chunk).

## (b) all_tafsirs boşaltma: chunk'lama BOŞALTMAZ; değişmez ayrı korunur

- **GLOBAL çift-durum = 0.** Hiçbir satırda hem `all_tafsirs.embedding` dolu HEM chunk'ı yok. Parent ve chunk **karşılıklı dışlayan**.
- Mekanizma: `generate-chunks.ts` all_tafsirs.embedding'e **hiç dokunmaz** (yalnız tafsir_chunks'a yazar). Chunk'lı parent'lar boş kalır çünkü embedding üretici onları **atlar** (`NOT EXISTS chunk` filtresi) — hiç gömülmez. Sıra bozulup çift oluşursa `fix-chunk-parent-embeddings.ts` parent embedding'ini aktif NULL'a çeker ("retrieval'da yalnızca tafsir_chunks yoluyla yarışırlar").
- **§4.1 sonucu:** whole-vs-chunk yarışı fiilen **yaşanmaz**. Her müfessir-ayet tek kaynaktan puanlanır: kısa → yalnız parent, uzun → yalnız chunk. Dedup `DISTINCT ON (mufassir_id, verse_id)` yalnız aynı uzun şerhin **chunk'ları arasında** (en yakın chunk = o müfessirin temsilcisi) seçim yapar; parent ile chunk arasında değil.

### Koşum ayetleri × Core-5 kaynak kırılımı
- yalniz parent (kısa, all_tafsirs.embedding): 20-5 hepsi; 24-35 Zemahşerî(3379)+İbn Kesîr(11110); 27-18 hepsi; 27-19 hepsi; 3-7 Zemahşerî(2673). = 15 hücre
- yalniz chunk (uzun, tafsir_chunks): 24-35 Taberî(22293,3)/Râzî(40488,5)/Kurtubî(16426,3); 3-7 Taberî(42379,5)/Râzî(30974,4)/Kurtubî(19598,3)/İbn Kesîr(16392,2). = 10 hücre
- çift = 0, vektörsüz = 0.
