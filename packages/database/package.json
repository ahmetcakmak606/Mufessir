{
  "name": "database",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "embed": "tsx scripts/embed-tafsirs.ts",
    "generate": "prisma generate --schema prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.4.6",
    "openai": "^4.28.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
